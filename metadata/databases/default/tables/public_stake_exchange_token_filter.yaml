table:
  name: stake_exchange_token_filter
  schema: public
object_relationships:
  - name: account
    using:
      foreign_key_constraint_on: account_id
  - name: offer
    using:
      foreign_key_constraint_on: offer_id
select_permissions:
  - role: public
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
