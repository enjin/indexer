table:
  name: account_token_event
  schema: public
object_relationships:
  - name: collection
    using:
      foreign_key_constraint_on: collection_id
  - name: event
    using:
      foreign_key_constraint_on: event_id
  - name: from
    using:
      foreign_key_constraint_on: from_id
  - name: to
    using:
      foreign_key_constraint_on: to_id
  - name: token
    using:
      foreign_key_constraint_on: token_id
select_permissions:
  - role: public
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
