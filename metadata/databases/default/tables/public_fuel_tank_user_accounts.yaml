table:
  name: fuel_tank_user_accounts
  schema: public
object_relationships:
  - name: account
    using:
      foreign_key_constraint_on: account_id
  - name: tank
    using:
      foreign_key_constraint_on: tank_id
select_permissions:
  - role: public
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
