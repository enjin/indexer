import * as events from '~/pallet/nomination-pools/events'
import * as calls from '~/pallet/nomination-pools/calls'
import * as storage from '~/pallet/nomination-pools/storage'
import * as constants from '~/pallet/nomination-pools/constants'
import * as processors from '~/pallet/nomination-pools/processors' 

export default {
    events,
    calls,
    storage,
    constants,
    processors,
}
