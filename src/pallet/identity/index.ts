import * as events from '~/pallet/identity/events'
import * as calls from '~/pallet/identity/calls'
import * as storage from '~/pallet/identity/storage'
import * as processors from '~/pallet/identity/processors'

export default {
    events,
    calls,
    storage,
    processors,
}
