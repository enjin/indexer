import * as events from '~/pallet/claims/events'
import * as storage from '~/pallet/claims/storage'
import * as processors from '~/pallet/claims/processors'

export default {
    events,
    storage,
    processors,
}
