import { CommonContext, BlockHeader, EventItem } from '../../types/contexts'

export async function token_account_updated(ctx: CommonContext, block: BlockHeader, item: EventItem, skipSave: boolean) {
    return undefined
}
