import { CommonContext, BlockHeader, EventItem } from '../../types/contexts'

export async function collection_deposit_recalculation_in_progress(
    ctx: CommonContext,
    block: BlockHeader,
    item: EventItem,
    skipSave: boolean
) {
    return undefined
}
