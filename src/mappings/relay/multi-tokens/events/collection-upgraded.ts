import { CommonContext, BlockHeader, EventItem } from '../../types/contexts'

export async function collection_upgraded(ctx: CommonContext, block: BlockHeader, item: EventItem, skipSave: boolean) {
    return undefined
}
