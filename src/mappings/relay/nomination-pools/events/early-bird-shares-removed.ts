import { CommonContext, BlockHeader, EventItem } from '../../types/contexts'

export async function earlyBirdSharesRemoved(ctx: CommonContext, block: BlockHeader, item: EventItem) {
    return undefined
}
